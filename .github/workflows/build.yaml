name: Reuse-build

on:
  workflow_dispatch:

jobs:
  Build:
    uses: kdemonie/actions-sandbox/.github/workflows/build.yml@v1
    with:
      RELEASE_CYCLE: ${{ vars.BTP_SUBACCOUNT }}
      ENVIRONMENT: "STAGING"
    secrets:
      inherit
  
